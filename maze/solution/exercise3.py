# The instruction list can hold other lists as well. An int will work with a
# sub-list the same way it will work with a single instruction, repeating the
# sub-list the specified number of times.
instructions = []
